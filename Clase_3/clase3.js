var edad = 24

// Incrementar en 1
edad += 1

var peso = 80

// Disminuir en 2
peso -= 2

var pan = 1 

peso += pan

var futbol = 3
peso -= futbol

var precioVino = 50.527
var total = Math.round(precioVino * 100 * 3 ) / 100 
//Limitar decimales 
var totalStr = total.toFixed(3)

// Pasar a decimal un string
var total2 = parseFloat(totalStr)

var pizza = 8 
var per = 2

var cant = pizza / per